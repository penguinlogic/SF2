function Z = DWT_full(X,levels,steps)

Y = DWT_iter(X,levels);
[Yq entropy_array total_bits] = DWT_quantise(Y,levels,steps);
Z = IDWT_iter(Yq, 4);

total_bits
entropy_array
avg_entropy = total_bits / 


% show ent per bit, total bits, error


end